name: warehouse_admin
version: 1.0.0+1
publish_to: 'none'

description: A modern Flutter Web admin dashboard for Inventory and Warehouse Management System

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  fl_chart: ^0.68.0
  intl: ^0.19.0
  go_router: ^13.2.0
  mobile_scanner: ^6.0.2
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
